// config.js

module.exports = {
    // JWT secret key used for signing the token
    JWT_SECRET: 'your-secret-key', // Replace with a more secure secret in production
  
    // Example MySQL database connection string (update with your own details)
    DB_CONNECTION_STRING: 'mysql://priya-agrawal:@localhost:3306/hotelmanagementsystem', // Replace with actual DB credentials
  
    // You can add more configuration options as required for your application
  };
  